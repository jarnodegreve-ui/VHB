/**
 * Verhuisd naar de lagenstapel (src/lib/lagen.ts, polish P2a): één stapel
 * voor Escape, de terugknop en het gebundeld opruimen van history-entries.
 * Deze module blijft als ingang voor bestaande imports.
 */
export { useHistoryDismiss, useLaag, verwerkOverlayPop } from './lagen';
